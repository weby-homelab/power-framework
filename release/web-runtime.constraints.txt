# Canonical Web Profile B lock entry point.
# The included requirements file is generated from uv.lock with full hashes.
# Keep this include path stable for Docker and release policy checks.
-r web-runtime.requirements.txt
