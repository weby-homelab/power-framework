# POWER 3.8 — P38-WP01-R1 Closure Reconciliation Handoff (PR-R1C)

```text
HANDOFF_SCHEMA: power.handoff.v2
CREATED_AT_UTC: 2026-09-16T13:00:00Z
GATE: P38-WP01-R1 Phase 5C SearchScope Closure Correction (PR-R1C closure)
REPOSITORY: https://github.com/weby-homelab/power-framework
BRANCH: docs/p38-wp01-r1-closure
BASE_SHA: 999f8c04a9cafd39deb9de2716e5cd07ec3e60b6
BASE_TREE: 9da812c49d2c9f43030656f6655d8e541bfe7d7c
STATUS: CLOSED / MERGED / VERIFIED (pending PR-R1C protected merge + readback)
```

## 1. Gate progression

```text
PHASE 5C / P38-WP01: CLOSED / MERGED (corrected by R1)
P38-WP01-R1: CLOSED / MERGED / VERIFIED (PR #437 runtime + this PR-R1C docs)
PHASE 5C EFFECTIVE STATUS: CLOSED / VERIFIED AFTER R1 CORRECTION
PHASE 5D / P38-WP02: READY FOR SEPARATE ADMISSION / NOT STARTED
POWER 3.8.0: NO-GO
```

## 2. Old incorrect/ambiguous claims vs correct live truth (history not rewritten)

- **Check accounting:** old `12/12 SUCCESS including CodeRabbit` → correct 11
  required contexts (no CodeRabbit); PR-head runs show 11 required SUCCESS plus
  optional `deploy` SKIPPED. Never equate total runs with required checks.

- **PR-C merge SHA:** old narrative `319e610d…` (nonexistent object) → correct
  live merge `319e6101e6c96f04d532de67a6f9e8f2aee4fa73` (tree `1768fb…`,
  parents `294b483…` and `708b367…`).

- **Baseline numbers:** old narrative 50s → canonical JSON 20/30/30/20/30
  (FTS/TF/Dense/Reranker/Fallback). Resolved against committed JSON.

- **Graph-zero:** old `OUT_OF_SCOPE_GRAPH_HOPS = 0` inferred from final results
  without traversal instrumentation; post-fix JSON had no such metric. R1
  corrected with executable evidence: eligible-before-read/build/traversal,
  OOS reads 0, queue 0, bridge impossible, hops 0 (`phase5c_r1_verification.json`
  graph section + 29 R1 tests).

- **R1 corrected evidence:** scope algebra AND (OR-within/AND-across, monotonic,
  digest distinct); FTS/TF/Dense/Reranker selected OOS 0 (real SQLite rows);
  fallback OOS reads 0 (pre-read index check, fail-closed); recall 1.0;
  p50 5.58ms / p95 8.5ms / peak 40.77MB (single-query; full-suite peak differs).

Old files (`PHASE_5C_BASELINE.md`, `PHASE_5C_REPORT.md`, old handoffs, old JSON)
remain historical.

## 3. Verified tuples

- PR-R1A #436: base `319e6101…`, head `5ebc13d3…`, merge `a0ee8eea…` (tree
  `629688d…`, parents `319e6101…` + `5ebc13d…`, verified valid, 11/11 required).
- PR-R1B #437: base `a0ee8eea…`, head `d6003211…`, merge `999f8c04…` (tree
  `9da812c…`, parents `a0ee8eea…` + `d6003211…`, verified valid, 11/11 required,
  full hermetic 2087 passed).
- PR-R1C (this branch): base `999f8c04…`, docs/governance only, no runtime.

## 4. Next authorized gate

```text
NEXT: P38-WP02 / Phase 5D RetrievalPlanner + ContextPack Read-Only Vertical Slice
```

Phase 5D must independently resolve the ContextPack `implementation_status`
`planned` vs runtime-complete contract question (no `model_construct` bypass,
no fake planned pack, no caller-controlled completion) and preserve the
Phase 5D (planner + read-only core) vs Phase 5G (MCP surfaces) boundary. Do not
begin 5D in this session.
